# miscellaneous configs
dhcp -> isc-dhcp-server - ip reservations (add `include "/usr/local/etc/dhcp/reservations.conf";` to dhcpd.conf)<br>
dnsmasq -> dns<br>
php -> tweaks<br>
rc.local -> custom startup sequence<br>
sysctl & udev -> tweaks
